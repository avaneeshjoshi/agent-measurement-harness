"""harness/report — the first-look view.

Presentation ONLY over the extracted + derived trees under ~/.caliper (dashboard/README
rule: computes nothing new; every number traceable to a record on disk).
Output is a single self-contained HTML file in the gitignored extraction
tree, because it renders local-only project display names.
"""

REPORT_VERSION = "first-look-0.1.0"
